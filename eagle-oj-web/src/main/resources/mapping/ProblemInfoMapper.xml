<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="problemInfo">
    <insert id="insertProblemInfo" parameterType="ProblemInfoEntity">
        INSERT INTO `problem_info` (`pid`, `belong`) VALUES (#{pid}, #{belong})
    </insert>

    <select id="getProblemInfoByPid" >

    </select>


</mapper>
